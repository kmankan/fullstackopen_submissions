// npm run server needs to be run first so that the json server can be created.
// server script command was set manually in the package.json file
import ReactDOM from 'react-dom/client'
import App from './App'

ReactDOM.createRoot(document.getElementById('root')).render(
  <App/>
)